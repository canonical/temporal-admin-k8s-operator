# Copyright 2026 Canonical Ltd.
# See LICENSE file for licensing details.

cli:
  description: Run the temporal cli command.
  params:
    args:
      type: string
      description: The command line arguments.
  required:
  - args

setup-schema:
  description: Initialize a new database or update existing Temporal schemas to this charm's version.

pre-upgrade-check:
  description: Check current core and visibility schema versions and migration ownership. Does not verify backup availability or restorability.
